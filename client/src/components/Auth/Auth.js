import React from 'react'
import { Avatar, Button, Paper, Grid, Typography, Container } from '@mui/material';

const Auth = () => {

    return (
        <div>
            Auth
        </div>
    );
};

export default Auth